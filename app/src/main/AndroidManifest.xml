<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.recehin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Recehin">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Recehin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".OnboardingActivity" />
        <activity android:name=".HalamanLoginActivity" />
        <activity android:name=".HalamanRegisterActivity" />
        <activity android:name=".HalamanLupaPasswordActivity" />
        <activity android:name=".BerandaActivity" />
        <activity android:name=".DaftarTransaksiActivity" />
        <activity android:name=".PengaturanActivity" />
    </application>
</manifest>